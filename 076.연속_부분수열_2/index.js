function solution(m, arr) {
  const len = arr.length;
  let count = 0;
  let p1 = 0;
  
  while(p1 < len) {
    let p2 = p1;
    let sum = 0;
    while(p2 < len && sum < m)
  }


}

let a = [1, 3, 1, 2, 3];
console.log(solution(5, a));
